# Service1 Configuration
########################

server:
  port: 8002

spring:
  application:
    name: mspoc-service2 # used for gathering configuration from configuration server
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888

# Actuator
management:
  endpoints:
    web:
      exposure:
        include: '*'
